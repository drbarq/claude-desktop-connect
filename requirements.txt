# Core dependencies for the Bedrock proxy
fastapi>=0.104.0
uvicorn>=0.24.0
boto3>=1.34.0
pydantic>=2.5.0

# Optional: For traffic interception (Approach 2)
mitmproxy>=10.1.0

# Optional: For the Streamlit chat UI (Approach 3)
streamlit>=1.29.0

# Optional: For testing with Anthropic SDK
anthropic>=0.18.0

# HTTP client for async requests
aiohttp>=3.9.0
